# Mismo proyecto de LiveKit que usa el agente (ver ../.env.example).
# Estos tres nunca se exponen al navegador: solo los lee app/api/token/route.ts
# en el servidor para firmar el access token.
LIVEKIT_URL=wss://tu-proyecto.livekit.cloud
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
